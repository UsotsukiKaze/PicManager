# Pic_old

This directory stores legacy source snapshots that are no longer imported by the running application.

The active route implementation now lives under `app/routers/*_api` and `app/routers/integrations`.
The files in `legacy_routers` are retained only for comparison and rollback reference.